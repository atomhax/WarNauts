using UnityEngine;
using System.Collections;

public class previewBrain : MonoBehaviour {

	public int type ;
	// Use this for initialization
	/*void Start () {
		gameObject.GetComponent<SpriteRenderer>().sprite = Resources.Load<Sprite> ("Unit" + (type+1).ToString ());
	}*/
	
	// Update is called once per frame
}
